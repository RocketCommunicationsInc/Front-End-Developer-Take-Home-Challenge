import { AlertSortPipe } from '@grmAlerts/pipes/alert-sort.pipe'

describe('AlertSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertSortPipe()
    expect(pipe).toBeTruthy()
  })
})
