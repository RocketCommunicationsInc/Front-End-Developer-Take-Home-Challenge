import { AlertErrorTimePipe } from './alert-error-time.pipe'

describe('AlertErrorTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AlertErrorTimePipe()
    expect(pipe).toBeTruthy()
  })
})
