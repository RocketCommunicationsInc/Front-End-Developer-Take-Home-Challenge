import { AlertFilterPipe } from '@grmAlerts/pipes/alert-filter.pipe'

describe('AlertFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertFilterPipe()
    expect(pipe).toBeTruthy()
  })
})
