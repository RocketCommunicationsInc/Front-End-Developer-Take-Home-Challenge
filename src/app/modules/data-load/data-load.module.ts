import {NgModule} from "@angular/core";
import {CommonModule} from "@angular/common";

/**
 * Sample module defining some injectable interfaces that can be used to load data and to define the paths that data are
 * located at.
 */
@NgModule({
  imports: [
    CommonModule
  ]
})
export class DataLoadModule {
}
