import { ContactErrorTimePipe } from './contact-time.pipe'

describe('ContactErrorTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactErrorTimePipe()
    expect(pipe).toBeTruthy()
  })
})
