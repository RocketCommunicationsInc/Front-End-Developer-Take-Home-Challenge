import { ContactSearchPipe } from './contact-search.pipe';

describe('ContactSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
